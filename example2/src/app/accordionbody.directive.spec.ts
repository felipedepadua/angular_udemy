import { AccordionbodyDirective } from './accordionbody.directive';

describe('AccordionbodyDirective', () => {
  it('should create an instance', () => {
    const directive = new AccordionbodyDirective();
    expect(directive).toBeTruthy();
  });
});
